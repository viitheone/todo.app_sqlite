{
  "name": "node_project2",
  "version": "1.0.0",
  "description": "Full stack todo app using NodeJS backend, SQLite database and JWT authentication.",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "dev" : "node --watch --env-file=.env --experimental-sqlite ./src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2"
  }
}
